// src/components/features/ViewerCount.tsx
'use client';

interface ViewerCountProps {
  videoIds: string[];
}

export function ViewerCount({ videoIds }: ViewerCountProps) {
  return <div>Viewer Count: </div>;
}