import { writeFile, readFile, mkdir } from 'fs/promises';
import { dirname } from 'path';

interface Args {
  model: string;
  input: string;
  output: string;
}

interface AnthropicResponse {
  content?: Array<{
    text?: string;
  }>;
  error?: {
    message: string;
    type?: string;
  };
