
interface GraphLink {
  source: string;
  target: string;
  strength: number;
}

interface GraphNavigationProps {
  nodes?: GraphNode[];
  links?: GraphLink[];
  className?: string;
  onNodeClick?: (node: GraphNode) => void;
}
